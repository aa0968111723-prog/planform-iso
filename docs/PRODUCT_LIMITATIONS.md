# 產品功能限制（1.0）

這份清單對齊產品裡「更多 → 已知限制」，也對齊
`src/core/productLimitations.ts`。

先前標成 1.1／只有欄位／mock 的入口已補上：

| 原缺口 | 現在 |
|---|---|
| 活動日期 | 卡片與分享 → 活動資訊可編 |
| 專案縮圖 | 儲存後自動畫預覽（約 1.2 秒後） |
| 這個專案的版本 | 綁定專案時寫入 `planform-iso:snapshots:<id>` |
| R-06 家族圈 | 排地墊 → 家族圈 |
| R-07 音控／控 PPT | 區域類型 + 目錄物件；E310 範例已放 |
| R-08 名牌 | 有報到桌就進物資清單；範例桌上有名牌盒 |
| 掃描場地 | 有照片用本機影像分析；沒照片才用示範偵測 |
| ✦ AI 幫我 | 沒金鑰離線規則；更多 → 雲端 AI 可貼 OpenAI 相容金鑰 |
| V2 報到精靈 | 動線 → A–E 模板 + 一鍵分流 |
| 兩個分頁 | 較新的 revision 不會被默默蓋掉；橫幅可載入最新或繼續用這一頁 |
| PWA | 更多 → 加到主畫面；本機 production preview 可離線 |

---

## 現況限制

### 線上 Zeabur 主機本環境連不到

雲端容器 egress 對 Zeabur 回 403。本機 `prodSmoke`（preview）與
「加到主畫面」已接上。遠端 production 要部署後在瀏覽器確認。

---

## 刻意不列在這裡的

- 期初茶會手作桌：用既有桌子表達，不另做 preset（R-11）。
- 沒有雲端帳號／同步：產品是 local-first，不是缺口。
