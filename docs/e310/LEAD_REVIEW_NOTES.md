# Lead 親審筆記（2026-08-21，E310 golden scenario 實測）

RC `551439e` + WP-A，桌機 1366×1024，八種匯出全部成功、console 零錯誤。
以下待併入 WP-C / WP-D 批次（皆 P2，除非另標）：

1. **入場動線末端戳進巧拼區約 0.5m**（quickStart.ts routeStops 最後一點
   `max(c.z+3.7, …)` 在 field anchorZ 3.2 時吃進第一排）。
   改為 field 前緣 −0.3m。→ WP-D 或整合時 Claude 自修。
2. **背包長桌突出區塊下緣**且視覺上與鞋子區桌形交錯——把 backpack zone 深度/位置
   對齊桌列（zone 貼牆、桌在 zone 內）。
3. **場佈總覽圖逐格編號 A-01…A-48/B-48 對巧拼是噪音**：field 模式匯出改
   「區塊標示＋格數（8×6，48 片）」，不逐格編號；夥伴圖已正確不編號。
4. **桌上物「電腦/收費」標籤白字在淺色桌面上看不清**（= audit E1 的實證）→ WP-C #1。
5. 走廊路線名稱「E310 入場動線」與站點 chip 在編輯器內重疊 → 小位移即可。
6. 夥伴圖動線徽章 2/3（引導/排隊點）無文字說明——考慮在 route/partner 匯出
   為 waypoint 加小字（引導、排隊）——擇簡，非必要。

已驗證良好：一鍵範例整體佈局合理；6+2 種匯出齊全；「尺寸待現場校正」footer 出現；
物資數量正確（96 巧拼、2 桌、講台/講桌/電腦/收費箱各 1）；夥伴圖起終編號動線清楚。

## WP-C 驗收（2026-08-21，親審 `_lead_wpc/exports/`）

**通過。** 巧拼區塊標示（A · 8×6 · 48 片）、入口 pill、紙面配色＋字級地板、
動線圖淡色畫巧拼/講台/家具、走廊服務鏈動線 起→…→終 全部到位；3D 匯出固定取景
完整含走廊、無裁切。

殘留 P2（先記錄，由 Grok R2 判定是否升級）：
1. 報到區/收費區 zone 名與桌面標籤（報到/收費）重疊。
2. 生活組區 label 貼到圖框右緣（長字未截斷）。
3. footer（圖例＋物資）變高 → 平面圖繪圖區縮小約 15%。
4. 3D 圖為深色主題輸出（可讀但與白底場刊風格不一致）；route 節點方塊偏大。
